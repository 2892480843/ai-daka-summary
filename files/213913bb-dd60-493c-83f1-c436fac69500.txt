第一轮
请帮我做一个HTML网页，是一个【24点挑战游戏】。

1. 一个数学思维训练游戏，随机给出4个数字，让用户用加减乘除算出24。

2. 用户打开页面 → 看到4个数字卡片 → 点击数字和运算符按钮组成算式 → 点击"提交" → 显示对错和正确答案 → 点击"下一题"。

3. 具体内容：随机生成1-13的数字（扑克牌范围），4个数字。确保题目有解（程序先验证有解再展示）。

4. 界面长什么样：页面居中，顶部显示"24点挑战"标题和当前得分。中间是4个数字卡片（大字号，彩色背景）。下方是运算符按钮（+ - × ÷ 和括号）。再下面是输入框显示当前算式，"提交"和"清空"按钮。底部有"提示"按钮（显示一种解法）和"下一题"按钮。

5. 额外功能：计时器（每题60秒倒计时）、难度选择（简单/普通/困难）、历史记录（显示最近10题的对错）。

请用纯HTML+CSS+JavaScript实现，不要接外部API，所有逻辑都在前端完成。代码要完整，保存为一个HTML文件就能直接打开运行。
第二轮：
双击运行后发现没有随机生成的1-13范围的4个数字；
第三轮：
双击运行后发现没有随机生成的1-13范围的4个数字。
谁能告诉我，为啥我的tokens这么快就没了